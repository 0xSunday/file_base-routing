import React from "react";
import { useRouter } from "next/router";
const slug = () => {
  const router = useRouter();
  return (
    <div>
      <h2>suni</h2>
    </div>
  );
};

export default slug;
